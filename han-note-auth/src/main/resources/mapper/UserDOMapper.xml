<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hanserwei.hannote.auth.domain.mapper.UserDOMapper">
    <select id="selectByPhone" resultType="com.hanserwei.hannote.auth.domain.dataobject.UserDO">
        select id, password
        from t_user
        where phone = #{phone}
    </select>

    <insert id="insert" parameterType="com.hanserwei.hannote.auth.domain.dataobject.UserDO" useGeneratedKeys="true"
            keyProperty="id">
        insert into t_user (hannote_id, password,
                            nickname, avatar, birthday,
                            background_img, phone, sex,
                            status, introduction, create_time,
                            update_time, deleted)
        values (#{hannoteId,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR},
                #{nickname,jdbcType=VARCHAR}, #{avatar,jdbcType=VARCHAR}, #{birthday,jdbcType=DATE},
                #{backgroundImg,jdbcType=VARCHAR}, #{phone,jdbcType=VARCHAR}, #{sex,jdbcType=TINYINT},
                #{status,jdbcType=TINYINT}, #{introduction,jdbcType=VARCHAR}, #{createTime,jdbcType=TIMESTAMP},
                #{updateTime,jdbcType=TIMESTAMP}, #{deleted,jdbcType=BOOLEAN})
    </insert>
</mapper>